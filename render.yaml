services:
  - type: web
    name: rigoloterias-api
    runtime: python
    plan: free
    region: oregon
    rootDir: backend
    buildCommand: pip install .
    # Free services do not support preDeployCommand; run the idempotent migration
    # immediately before starting this single-instance staging API.
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/ready
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: rigoloterias-db
          property: connectionString
      - key: WRITE_API_TOKEN
        generateValue: true
      - key: CORS_ORIGINS
        sync: false
      - key: TRUSTED_HOSTS
        sync: false
      - key: RATE_LIMIT_REQUESTS
        value: "120"
      - key: RATE_LIMIT_WINDOW_SECONDS
        value: "60"

  - type: web
    name: rigoloterias-web
    runtime: static
    rootDir: frontend
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        sync: false

databases:
  - name: rigoloterias-db
    plan: free
    region: oregon
    postgresMajorVersion: "16"
    databaseName: rigoloterias
    user: rigoloterias
